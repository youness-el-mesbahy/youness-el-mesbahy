---
import {MoveRight} from "@lucide/astro";
import Badge from "./Badge.astro";

const {project} = Astro.props
const {title, imageUrl, description, technologies} = project.data;
---

<div class="relative w-[328px] py-4 px-5 rounded-lg border border-gray-200">
    <img
            src={imageUrl}
            alt={title}
            width="288"
            height="162"
            class="w-[288px] h-[162px] aspect-video rounded-lg object-cover bg-gray-100"
            loading="lazy"
    />
    <h3 class="text-lg mt-4">{title}</h3>
    <p class="text-sm mt-[6px] font-light">
        {description}
    </p>
    <div class="mt-3 space-x-2">
        {technologies.map((technology) => (
                <Badge size="small">
                    <img src={technology.icon} width="14" height="14" class="inline-block"/>
                    <span>{technology.name}</span>
                </Badge>
        ))}
    </div>
    <div class="mt-2 invisible ">
        place
    </div>
    <div class=" absolute bottom-3 left-1/2 -translate-x-1/2">
        <MoveRight/>
    </div>
</div>

<script>

</script>