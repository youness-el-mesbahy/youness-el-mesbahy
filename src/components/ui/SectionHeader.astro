---
const {title, subtitle} = Astro.props
---

<header class="flex text-center flex-col items-center gap-2">
    <h2 class="text-xl font-semibold">{title}</h2>
    {subtitle && <p>{subtitle}</p>}
</header>
